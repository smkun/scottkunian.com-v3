// Firebase Functions placeholder
// Admin claim management will be done via Node.js script (scripts/setAdminClaim.js)
// See docs/ADMIN_SETUP.md for setup instructions

export const placeholder = () => {
  return { message: 'Functions not yet deployed - using script-based admin setup' };
};
